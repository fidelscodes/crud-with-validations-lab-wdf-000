<h2><%= @song.title %> by <%= @song.artist_name %></h2>
<%= link_to 'Edit', edit_song_path(@song) %>
<%# disable_with inside the data hash, disables 'Delete' link while submitting. This prevents duplicate requests which can cause errors %>
<%= link_to 'Delete', @song, method: :delete, data: { confirm: 'Are you sure? This action cannot be undone.', disable_with: 'Deleting...' } %>

<br>
<br>
<%= link_to 'Back to All Songs', songs_path %>
